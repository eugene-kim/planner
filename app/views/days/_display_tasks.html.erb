<h2 id="daily_list_<%= daily_list.id %>">
	<%= format_date(daily_list.date) %>
</h2>

<%= raw daily_list.tasks %>

<%= link_to "Show", day_path(daily_list) %> |
<%= link_to "Edit", edit_day_path(daily_list) %>